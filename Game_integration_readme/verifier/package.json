{
  "name": "boly-game-verifier",
  "version": "1.0.0",
  "private": true,
  "description": "Verify a game build correctly implements the Boly license heartbeat (ParamsCatcher) before uploading it to the platform.",
  "bin": {
    "boly-verify": "verify.js"
  },
  "scripts": {
    "start": "node verify.js",
    "build:bin": "npx -y pkg@5.8.1 verify.js --targets node18-win-x64 --out-path dist && npm run build:rename",
    "build:rename": "cd dist && mv -f verify.exe boly-verify-win.exe"
  },
  "engines": {
    "node": ">=18"
  }
}
